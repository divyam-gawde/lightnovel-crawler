version: "3.9"

services:
  lncrawl:
    image: your-image-name
    container_name: lncrawl
    restart: always
    command: python -m lncrawl server
    volumes:
      - ./data:/data
    ports:
      - "8000:8000"
